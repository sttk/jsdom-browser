'use strict'

const createBlankWindow = require('./create-window')
const promiseToLoadContent = require('./load-content')
const clearHistory = require('./clear-history')

module.exports = {
  createBlankWindow,
  promiseToLoadContent,
  clearHistory,
}
